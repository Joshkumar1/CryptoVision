services:
  - type: web
    name: cryptovision
    runtime: node
    plan: free
    buildCommand: npm install && npm --prefix server install && npm run build:all
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
